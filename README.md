
# 🚀 Fast Comand SM - نظام إدارة اللوجستيات

نظام متكامل لإدارة عمليات الشحن، الطلبات، والمخزون، مبني باستخدام React و Supabase.

## 📋 المتطلبات

*   Node.js (الإصدار 18 أو أحدث)
*   حساب Supabase (لقاعدة البيانات والمصادقة)
*   **لنسخة الموبايل:** Android Studio مثبت على جهازك.

## 🛠️ دليل التثبيت والتشغيل (GitHub)

1.  **استنساخ المستودع (Clone):**
    ```bash
    git clone https://github.com/your-username/fast-comand-sm.git
    cd fast-comand-sm
    ```

2.  **تثبيت الحزم (Dependencies):**
    ```bash
    npm install
    ```

3.  **إعداد متغيرات البيئة (.env):**
    *   قم بإنشاء ملف جديد في المجلد الرئيسي باسم `.env`
    *   انسخ المحتوى من ملف `.env.example`
    *   أضف بيانات مشروعك من Supabase:
    
    ```env
    VITE_SUPABASE_URL=https://xxxxxxxx.supabase.co
    VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
    ```

4.  **تشغيل التطبيق محلياً:**
    ```bash
    npm run dev
    ```

## ☁️ النشر (Deployment)

يمكنك نشر التطبيق بسهولة على **Vercel** أو **Netlify** أو **Cloudflare Pages**.
تأكد فقط من إضافة `Environment Variables` في لوحة تحكم الاستضافة بنفس الأسماء الموجودة في `.env`.

---

## 📱 تحويل التطبيق إلى APK (Android)

لتحويل المشروع إلى تطبيق أندرويد:

1.  بناء نسخة الويب:
    ```bash
    npm run build
    ```
2.  مزامنة ملفات Capacitor:
    ```bash
    npx cap sync
    ```
3.  فتح المشروع في Android Studio:
    ```bash
    npx cap open android
    ```

---

## 🗄️ إعداد قاعدة البيانات (لأول مرة)

عند تشغيل التطبيق وربطه بـ Supabase جديد:
1.  ستظهر شاشة تسجيل الدخول.
2.  إذا لم تكن الجداول موجودة، ستظهر رسالة خطأ.
3.  سجّل الدخول ببيانات المدير (أو أنشئ مستخدماً جديداً إذا سمحت الإعدادات).
4.  اذهب إلى **الإعدادات > المستخدمين > إعداد قاعدة البيانات**.
5.  انسخ كود SQL ونفذه في **Supabase SQL Editor**.

---
حقوق النشر محفوظة © 2024
